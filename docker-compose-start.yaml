services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
#    expose:
#      - "3000"
    environment:
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      SECRET_KEY_BASE: ${SECRET_KEY_BASE}